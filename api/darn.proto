syntax = "proto3";

package darn;

service ErrorIn {
    rpc Submit(Data) returns(Success) {}
}

message Data {
    string plugin = 1;
    string trigger = 2;
    string error = 3;
    string platform = 4;
}

message Success {
    bool success = 1;
}